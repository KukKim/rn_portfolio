import { CommonText, SafeAreaContainer } from "@kukkim/react-native-ui";
import { StyleSheet } from "react-native";

export default function UIScreen() {
  return (
    <SafeAreaContainer>
      <CommonText>UI</CommonText>
    </SafeAreaContainer>
  );
}

const styles = StyleSheet.create({});
